# Alembic configuration. The DB URL is resolved at runtime from app settings
# (env DATABASE_URL); see app/db/migrations/env.py. The line below is a fallback.
[alembic]
script_location = app/db/migrations
prepend_sys_path = .
sqlalchemy.url = postgresql+asyncpg://apex:apex@postgres:5432/apexmind

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
